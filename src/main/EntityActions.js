type EntityAction = {
    name: string,
    subject: Object,
    data: any
};

// export const MOVE_UP: EntityAction = {
//     name: 'MOVE_UP',
//     subject:
// };

// register an action to a key and action type (keyup/keydown)